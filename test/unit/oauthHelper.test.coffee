auth = require "./auth"

describe "OAuthHelper", ->
    
    describe "getAccessToken()", ->
        it "should return access point for user", (done)->
            auth.getAccessToken done
                                

                            
                
    
